import 'package:flutter/cupertino.dart';

class WeatherInit extends StatelessWidget {
  const WeatherInit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('請輸入關鍵字查詢天氣'));
  }
}
